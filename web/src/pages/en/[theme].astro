---
import View from "../../views/Theme.astro";
import { published } from "../../lib/themes";

export function getStaticPaths() {
  return published().map((theme) => ({ params: { theme: theme.slug.en }, props: { theme } }));
}
const { theme } = Astro.props;
---
<View lang="en" theme={theme} />
