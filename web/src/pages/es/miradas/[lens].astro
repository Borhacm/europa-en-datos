---
import View from "../../../views/Lens.astro";
import { LENSES } from "../../../lib/themes";

export function getStaticPaths() {
  return LENSES.map((lens) => ({ params: { lens: lens.slug.es }, props: { lens } }));
}
const { lens } = Astro.props;
---
<View lang="es" lens={lens} />
