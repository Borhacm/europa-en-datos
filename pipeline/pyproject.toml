[project]
name = "europa-en-datos-pipeline"
version = "0.1.0"
description = "Descarga y normaliza los datos abiertos que alimentan los gráficos de Europa en datos"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "pyreadr>=0.5",
]

[tool.uv]
package = false
